<?xml version="1.0" encoding="utf-8"?>
<!--
    Galaxy Shutter Mute Manager の AndroidManifest.xml
    APK ダウンロードのための INTERNET 権限と、
    Shizuku 経由でインストール・権限付与を行うための設定
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- GitHub Releases API への HTTP 通信に必要 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- APK を外部ストレージに保存するために必要（Android 9 以下） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GalaxyShutterMuteManager"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            ShizukuProvider: Shizuku バインダーを取得するために必要
        -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!--
            APK ダウンロード用 FileProvider
            Android 7+ では File URI を直接使えないため FileProvider 経由で共有する
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
